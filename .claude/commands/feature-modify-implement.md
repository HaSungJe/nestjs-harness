---
description: 작성된 change.md 기반으로 수정 구현·테스트·리포트 추기 진입
argument-hint: <featureName>
---

`$ARGUMENTS` 를 기능명(featureName)으로 보고, `.harness/docs/feature-modify-implement/index.md` 의 절차를 그대로 따른다.

이 슬래시 커맨드는 한국어 키워드 `<featureName> 수정 시작` 과 동등한 진입점이다.

⚠️ change.md 존재 + 결정사항 답변 완료가 전제. 위반 시 진입하지 않고 중단·안내. 완료 후 기존 report.md 끝에 `## 수정 - <YYMMDD>-<N>` 섹션을 추기한다.
