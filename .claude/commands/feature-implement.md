---
description: 기획된 work.md 기반으로 구현·테스트·리포트 진입 (전제 조건 4종 검증 후)
argument-hint: <featureName>
---

`$ARGUMENTS` 를 기능명(featureName)으로 보고, `.harness/docs/feature-implement/index.md` 의 절차를 그대로 따른다.

이 슬래시 커맨드는 한국어 키워드 `<featureName> 작업 시작` 과 동등한 진입점이다.

⚠️ **전제 조건 4종** (work.md 존재 / request 결정사항 답변 / 사전 구현 항목 완료 / validator 통과) 이 깨지면 ⑤단계 이하로 진입하지 않는다 — 무엇이 부족한지 사용자에게 안내 후 중단.
