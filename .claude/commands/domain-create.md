---
description: NestJS 도메인 스캐폴드 생성 (CLAUDE.md Architecture 규약 우선, 부재 시 fallback)
argument-hint: <domain>
---

`$ARGUMENTS` 를 도메인명으로 보고, `.harness/docs/domain-create/index.md` 의 절차를 그대로 따른다.

이 슬래시 커맨드는 한국어 키워드 `<domain> 도메인 생성` 과 동등한 진입점이다. 인자가 비어 있으면 사용자에게 도메인명을 되묻는다.
