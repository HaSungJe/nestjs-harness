services:
  redis:
    image: redis:7-alpine
    container_name: nest11-redis
    ports:
      - "${BULLMQ_REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

volumes:
  redis-data:
